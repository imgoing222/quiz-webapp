import { render } from "@testing-library/react";
import Quiz from "../Quiz";

test("무엇을 테스트할까", async () => {
	// 4개의 답안이 있는가?
	// 답안을 누를 시 모달이 열리는가?
	// 다음 문항 버튼이 모달에 있는가?
	// 다음 문항을 누르면 다음 문항으로 넘어가는가?
});

export {};
